{
  "name": "note-taker",
  "version": "1.0.0",
  "description": "Full stack note taker app that allows notes to be created and saved to a database",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Russell Dunn",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
